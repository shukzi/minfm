# Changelog

## Unreleased

- Restore the previous selector when returning to a parent directory.
- Add `/` search for matching entries in the current directory.
- Add `F` search across the usable filesystem with background scanning,
  cancellation, permission-error reporting, and a result limit.
- Open filesystem search results in their containing directory without an
  additional warning.
- Document the new search shortcuts in the footer and help popup.
